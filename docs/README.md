# Documentation

This directory is a Mintlify documentation project.

```bash
npm run docs:dev
```

The command uses the current Mintlify CLI through `npx`; no docs runtime is added to the published calendar package.
